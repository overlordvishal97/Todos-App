def mean(mylist):
    the_mean = sum(mylist)/ len(mylist)
    return the_mean
#the function has been made if u want to make a func it should start with 'def'
print(mean([1,4,5]))
#you can check if your function is working fine using 'type'.

#Importance of return in defining a function 
#if you want define a function and you want it to work properly then you have to use return function if you use any other function then it will output 'return'


